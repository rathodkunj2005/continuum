{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default app capability for the main Continuum window.",
    "windows": ["*"],
    "permissions": [
        "core:default",
        "shell:default"
    ]
}
